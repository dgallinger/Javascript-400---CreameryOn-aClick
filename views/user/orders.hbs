<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <h1>User Profile</h1>
        <hr>
        <h2>My Orders</h2>
        {{# each orders }}
            <div class="panel panel-default">
                <div class="panel-body">
                    <ul class="list-group">
                        <strong> Order Number : {{this._id}} </strong>
                        <a href="/user/profile/orders/{{this._id}}"><b style ="text-align: left">Get Details</b></a> 
                        <b><h5>User ID: {{this.userId}}</h5></b>
                        
                      
                        {{# each this.items }}
                            <li class="list-group-item">
                                <span class="badge"> ${{ this.price }}</span>
                                {{ this.title }} || <strong> {{ this.quantity }} </strong>
                            </li>
                        {{/each}}
                    </ul>
                </div>
                <div class="panel-footer">
                    <strong>Total Price: ${{ this.total }}</strong>
                </div>
            </div>
        {{/each}}
    </div>
</div>